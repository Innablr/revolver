# yaml-language-server: $schema=../../revolver-config-schema.json
---
defaults:
  settings:
    region: ap-southeast-2
    timezone: Australia/Melbourne
    timezoneTag: Timezone
    organizationRoleName: none
    revolverRoleName: none
    localResourcesFile: ./test/plugins/powercycle.resources.json
    # Read list of Organisation accounts from a JSON generated by localOrgAccountsWriteFile
    # Meant for local testing when developing schedules and filters
    localOrgAccountsFile: ./test/plugins/org-accounts.json
    # Write a list of Organisation accounts to this file.
    # Meant for local testing when developing schedules and filters
    # localOrgAccountsWriteFile: ./test/plugins/org-accounts.output.json

    # resourceLog:
    #   json:
    #     file: ./test/plugins/resources.json
    #   csv:
    #     file: ./test/plugins/resources.csv
    #     reportTags: ["Name", "Schedule"]
    # auditLog:
    #   csv:
    #     file: ./test/plugins/audit.csv
    # excludeResources:
    #   - tag: 'Schedule|ignore'

  drivers:
    - name: ec2
      pretend: true
    - name: rdsInstance
      pretend: true
    - name: rdsCluster
      pretend: true


  plugins:
    powercycle:
      active: true
      configs:
        - tagging: strict
          availabilityTag: Schedule

organizations:
  - accountId: "001122334455"
    settings:
      name: some-org-name
#       stuff: 123 # ignored
#       region: whatever # override default
#       # revolverRoleName from defaults
#   - accountId: "111111111111"
#     settings:
#       region: eu-west-1
#       name: eh-global-euw1

accounts:
  includeList:
    - accountId: "112233445566"
      settings:
        name: whatdev
  # excludeList:
    # - accountId: "665544332211"
      # settings:
        # name: whatprod
